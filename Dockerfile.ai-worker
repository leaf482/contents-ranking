FROM golang:1.23-alpine AS builder
WORKDIR /app

COPY go.mod go.sum* ./
RUN go mod download

COPY . .
RUN go mod tidy && CGO_ENABLED=0 go build -o /ai-worker ./cmd/ai-worker

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=builder /ai-worker /ai-worker
ENTRYPOINT ["/ai-worker"]
